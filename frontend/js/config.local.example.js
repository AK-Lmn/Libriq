/* ============================================
   LIBRIQ LOCAL CONFIG EXAMPLE
   Copy to `config.local.js` and keep it uncommitted.
   Or inject the same object at deploy time as
   `window.LibriqConfig`.
   ============================================ */

window.LibriqConfig = window.LibriqConfig || {
  googleBooksApiKey: '',
};
